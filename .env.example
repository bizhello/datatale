# Planned server-only configuration. Bootstrap does not make AI requests.
# Never use NEXT_PUBLIC_ for provider credentials. Keep local values in .env.local.
OPENAI_BASE_URL=https://ai-gateway.spiro.vc/v1
OPENAI_API_KEY=
AI_MODEL=gpt-5.6-terra
