# Planned configuration. Bootstrap does not make AI requests.
# Pick ONE route when implementing AI. Never use NEXT_PUBLIC_ for secrets.
AI_GATEWAY_API_KEY=
# Alternative: direct OpenAI API, separate API billing.
OPENAI_API_KEY=
# Select a supported structured-output model after fixture evaluation.
AI_MODEL=
